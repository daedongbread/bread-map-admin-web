import React from 'react';
import { TextField } from '@/components/BakeryDetail/Form/TextField';

export const CurationBakery = () => {
  return <div>{/*<TextField label={'추천이유'} name={} value={} onChangeForm={} />*/}</div>;
};
